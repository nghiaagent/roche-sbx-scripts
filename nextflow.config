// nextflow.config

env {
    // Reads $MYSCRATCH from your terminal and appends the cache structure
    REF_CACHE = "${System.getenv('MYSCRATCH')}/\\%2s/\\%2s/\\%s"
}

process {
  cache = 'lenient'
}
